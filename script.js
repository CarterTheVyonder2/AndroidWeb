
// JavaScript for simple interactivity and animations

// Placeholder for widget click functionality
document.querySelectorAll('.widget').forEach(widget => {
    widget.addEventListener('click', () => {
        alert('Widget clicked!');
    });
});

// Placeholder for app dock functionality
document.querySelectorAll('.app-dock .icon').forEach(icon => {
    icon.addEventListener('click', () => {
        alert('App opened!');
    });
});
